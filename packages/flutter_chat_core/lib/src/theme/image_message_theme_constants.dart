import 'dart:ui';

const defaultImagePlaceholderColor = (
  light: Color(0xFFEEEEEE),
  dark: Color(0xFF111111),
);

const defaultImageDownloadProgressIndicatorColor = (
  light: Color(0xCC101010),
  dark: Color(0xCCEEEEEE),
);

const defaultImageUploadProgressIndicatorColor = (
  light: Color(0xCC101010),
  dark: Color(0xCCEEEEEE),
);

const defaultImageUploadOverlayColor = (
  light: Color(0x80EEEEEE),
  dark: Color(0x80111111),
);
